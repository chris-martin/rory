dependencies:
  cache_directories:
    - ~/nix-store
    - ~/.stack
    - ~/rory/.stack-work
    - ~/rory/rory/.stack-work
  override:
    - ./circle-setup.sh
    - ./circle-build.sh

test:
  override:
    - ./circle-test.sh
